import {View, Text} from 'react-native';
import React from 'react';

type Props = {};

const Sliders = ({}: Props) => {
  return (
    <View>
      <Text>Sliders</Text>
    </View>
  );
};

export default Sliders;
